// import fs from "node:fs";

// const input = JSON.parse(fs.readFileSync("input.json", "utf8"));
// const rules = JSON.parse(fs.readFileSync("rules-test.json", "utf8"));

// for (const line of input) {
//   //   console.log(line);
//   console.log(line.length % 2 === 1 ? "Y" : line);
// }

console.log(
  [1, 2, 3, 4, 5].sort((a, b) => {
    return -1;
  })
);
